interface Type {
  [key: string]: string;
}

const Icon: Type = {
  fontSize: '20px',
  cursor: 'pointer',
  color: '#888888',
  // marginLeft: ' 18px',
  marginBottom: ' -9px',

};

const classes = { Icon };

export default classes;
