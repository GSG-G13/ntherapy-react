interface Type {
  [key: string]: string;
}

const Icon: Type = {
  fontSize: '20px',
  cursor: 'pointer',
  color: '#888888',
  marginBottom: '10px',
};

const classes = { Icon };

export default classes;
